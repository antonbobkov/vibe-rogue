# DECISIONS

Ambiguity log (`40-implementation-plan.md` § PLN-07.1). One row per decision taken where a rule was
missing, ambiguous, or self-contradictory. Rows are append-only; IDs are never reused or renumbered.

| ID | Spec IDs | Question | Decision | Why | Milestone |
|---|---|---|---|---|---|
| D-001 | PLN-02 R2, TEC-02 | R2 exempts "the storage adapter in `save.js`", not the whole file. How does `dom-free.test.js` express a partial exemption? | `src/save.js` is scanned like any other core module, but the single identifier `localStorage` is permitted in it. `src/render.js`, `src/main.js` and `src/screens/*` stay fully exempt. | Keeps R2 enforced over the rest of `save.js` (serialize/deserialize must stay headless) while allowing exactly the adapter TEC-09 requires. | M00 |
| D-002 | PLN-02 R2, R3 | Should the R2/R3 scanners match forbidden text inside comments and string literals? | No. Both scanners strip comments and string bodies before matching; `${...}` interpolations inside template literals are kept, because they are code. | Item and script data (`data/*.js`, `SCR-10` templates) contain English prose that would otherwise trip on words like `function`, `class` or `window`, producing false failures that R10 forbids fixing by renaming spec content. | M00 |
| D-003 | PLN-04, PLN-06 M00 | `npm test` must run the unit, integration and meta tiers but not the Playwright specs. `node --test test/` cannot express that. | `npm test` runs `node tools/dod.js --node`, which discovers `test/**/*.test.js` itself and passes the explicit file list to `node --test`. | `node --test <dir>` treats *every* `.js` file under a directory named `test` as a test file, so it would try to execute `test/e2e/*.spec.js` and `test/fixtures/*.js`. Positional glob patterns need Node ≥ 22, but PLN-04 fixes CI at Node 20. | M00 |
| D-004 | PLN-04 | PLN-04 describes the DoD as "`node --test` filtered by milestone tag, then the meta tests". Are the meta tests included in the filtered run as well? | No. Step 1 runs `test/**/*.test.js` outside `test/meta/` with `--test-name-pattern`; step 2 runs `test/meta/*.test.js` with no filter. | The repo rules (R2, R3, R5, ACC coverage) always apply in full, and splitting the two steps avoids running the meta tier twice per DoD. | M00 |
| D-005 | PLN-02 R4, PLN-07.4 | R4 requires an exact `@playwright/test` pin but names no version. | Pinned `1.63.0`. | Latest stable at scaffold time; every `playwright < 1.55.1` carries a published advisory (GHSA-7mvr-c777-76hp), so the pin also keeps `npm audit` clean. PLN-07.4's downgrade path was not needed — the Chromium download succeeded. | M00 |
| D-006 | TEC-08, ENM-08, PLN-06 M01 | The plan fixes the names in `grid.js` but not the shapes: what is a tile, what does `bfs` return, and what exactly is in an `astar` path? | A tile is `{x, y}`; a tile index is `idx(x, y) = y * 60 + x`. `bfs(passable, starts)` returns an `Int32Array(W * H)` of step distances with `-1` for unreachable, and accepts one start or many. `astar(passable, from, to, {maxLen})` returns the steps **after** `from` up to and including `to` (`[]` when `from === to`, `null` when there is no path within the cap). `neighbors8` is clipped to the map and returned in reading order. Passability is always the step callback `(from, to) => boolean`. | The step callback is the only shape that can carry the ENM-08 door-diagonal rule, `opensDoors`, hazards and occupancy without `grid.js` knowing what a tile is — ENM-08 makes all four caller-specific. "Steps after `from`" is what ENM-06's "take the first step of the path" needs (`path[0]`), and the typed array keeps the WLD-11 step-5 room BFS allocation-free. | M01 |
| D-007 | TEC-08, CMB-08 | TEC-08's Bresenham excludes the start and includes the end. What does a degenerate line (start = end) return? | `[{x0, y0}]` — the single tile. | It is what the algorithm as written does: it stops "once `(x1, y1)` has been emitted", and with `dx = dy = 0` neither branch fires, so the first emission is the start tile, which is also the end tile. "End included" wins over "start excluded" because CMB-08 uses the path to find what a projectile hits, and a projectile aimed at your own tile lands on it. | M01 |
| D-008 | WLD-05, TEC-08, ACC-78 | ACC-78 requires the FOV to be symmetric, but the reference symmetric-shadowcast reveals a *wall* whenever a scan line touches it, without the `is_symmetric` check — so wall visibility is not symmetric. Which tiles does the symmetry claim cover? | Symmetry is asserted over pairs of non-blocking tiles, which is exactly what WLD-05's "Tick sees the enemy ⇔ the enemy's shadowcast reaches Tick" needs, since actors only ever stand on passable tiles. Walls are revealed by the reference's rule so that "walls at the edge are visible" (ACC-78) holds. Tiles off the map are treated as blocking, and the scan stops past the radius, which is identical to computing the full cast and then applying the Chebyshev filter (within a quadrant the scan depth *is* the Chebyshev distance). | This is the reading under which every clause of ACC-78 passes at once; the alternative (running the symmetry check on walls too) makes room walls flicker in and out and contradicts WLD-05's "the wall you can see is drawn". | M01 |
| D-009 | ITM-10, PLN-06 M01 | A loot table is a list of `(itemName, weight)` pairs. Does `weighted(rng, table)` return the pair or the name? | The entry's first element — the item name. Weights must be positive integers, and exactly one draw is consumed. | Every caller in `12` and `21` wants the name; returning the pair would make each call site index into it. Rejecting non-positive weights turns a mistranscribed table (M02) into a loud failure rather than a silent bias. | M01 |
| D-010 | TEC-03, TEC-07 | TEC-03 gives three dice spellings (`2d4+1`, `1 (flat)`, `1`) but no grammar; `23-floors.md` also uses `0d1+1`. | `parseDice` accepts `NdS`, `NdS+M`, `NdS-M`, a bare signed integer, and an integer followed by `(flat)`, case-insensitively and ignoring surrounding whitespace; anything else throws. Both flat spellings give `{n: 0, sides: 1, mod: value}`, so `roll` consumes no draw for them. A `Dice` object is passed through unchanged. | TEC-03 fixes flat `1` as `{0, 1, 1}` and TEC-07 fixes `roll` as "sum of `n` calls plus `mod`", so `n = 0` is the only encoding of a flat value that spends no randomness — which the call-order guarantee of TEC-07 depends on. Throwing on anything else keeps a typo in the M02 transcription from silently becoming `1d1`. | M01 |
| D-011 | WLD-05, CMB-08, TEC-08, TEC-11 | WLD-05 and CMB-08 both cite `TEC-11` for the exact FOV and Bresenham variants, but `TEC-11` is Input; the algorithms are in `TEC-08`. | Read both as `TEC-08` and corrected the two cross-references in the prose (PLN-07.1 step 3). No rule, number or ID changed. | `TEC-08` is titled "Deterministic algorithms" and contains both the Bresenham form and the Albert Ford FOV reference the two sentences describe; `TEC-11` contains neither. | M01 |
| D-012 | PLN-02 R3, PLN-06 M02 task 4, TEC-10 | M02 task 4 asks `data/palette.js` for `resolve(colorOrName)`, but R3 says modules under `data/` export frozen plain objects and contain no functions, and `test/meta/data-pure.test.js` enforces it mechanically. | `data/palette.js` holds only the frozen UI-08 table. A new module `src/palette.js` imports it and exports `resolve`, `resolves`, `background` and `dim`. R3 and its meta test are unchanged; the M02 prose was corrected to match (PLN-07.1 step 3). | R3 is a binding rule with a mechanical test behind it; the helper is one import away and `src/` is where TEC-02 already puts logic. Keeping the table in `data/` preserves TEC-02's file layout. | M02 |
| D-013 | UI-08 | Every UI-08 row is `name → #rrggbb` except `telegraph`, which is "`#ffffff` on background `#602020`" — two colors under one name. | `PALETTE.telegraph` is the foreground `#ffffff`; the background is `PALETTE_BG.telegraph = '#602020'`. | `resolve()` must return one color for one name, and UI-09 rule 4 uses the pair as a foreground/background swap, so the two halves are looked up separately by the renderer. | M02 |
| D-014 | TEC-03, PLN-06 M02 task 1 | TEC-03 types every `dice`, `attack`, `heavyAttack` field as a parsed `Dice` object, while M02 task 1 says "dice stay as strings in data (`parseDice` at load in M04)". | Dice are stored as the spec's own strings (`'2d4+1'`, `'1 (flat)'`, `'0 (flat)'`); `parseDice` (M01, D-010) converts them when actors are created in M04. | Strings are the verbatim transcription M02 is for, and they keep a typo visible in review; `parseDice` already throws on anything it does not recognise, so a bad string fails loudly at load. | M02 |
| D-015 | ITM-01, CAT-02, CAT-05 | `floors` is "the set of floors whose floor and cache tables may contain it", but the catalog writes "start only" for the Wrench and "drop: the Conductor" / "drop: the Regulator" for the two uniques. | Those three items have `floors: []`. | They never appear in a floor or cache table, which is exactly what an empty set means; ITM-01 already exempts enemy drop tables from `floors`, so the boss drops still work. A test asserts the converse — every name in a floor/cache table lists that floor. | M02 |
| D-016 | CAT-07, ITM-01, TEC-03 | CAT-07 lists the records as one row, "Journal page 1–8", so the nine record items have no individual names, and data keys must match the bold spec names exactly (`OVR-07` rule 5). | The nine records are named `Journal page 1` … `Journal page 8` and `Understudy Blueprint`, with `journalIndex` 1–8 on the pages and `blueprint: true` on the drawing. Each page's `floors` is the floor it is found on. | It is the only reading that gives CAT-08's "9 records" nine distinct entries, and it matches the row's own wording. Records never appear in a loot table, so no cross-reference depends on the spelling. | M02 |
| D-017 | ENM-01, TEC-03 | ENM-01 lists an enemy's ranged attack as separate `range`, `rangedAttack`, `windUp` and `noise` fields; TEC-03 groups them into one optional `ranged: { dice, range, windUp, ignoresPlating?, onHit?, noise }`. | TEC-03's grouped `ranged` object is what `data/enemies.js` writes. | TEC-03 is titled "Data shapes" and is normative for the builder; ENM-01 is describing which facts an enemy type carries, not their layout. Grouping also keeps `noise` unambiguous (it is the ranged action's noise, not the enemy's melee noise). | M02 |
| D-018 | BST-04, BST-05, BST-06, TEC-03 | Every boss's table row gives two speeds ("NORMAL (P1), FAST (P2)"), but `EnemyType.speed` is a single value. | `speed` is the boss's Phase 1 speed (Conductor NORMAL, Regulator SLOW, Understudy NORMAL); the later phases are set by the boss script in M08. | Phase 1 is the state a boss is created in, and BST-03 already makes phase transitions the script's job ("the new phase's script applies from the boss's next action"). | M02 |
| D-019 | BST-02, BST-06, CMB-10 | Two per-type numbers in `22` sit outside the bestiary table: the Rust-moth takes 4 per turn from Burning instead of 2, and the Understudy has its own spring of 100 that costs 2 per action. | They are fields on the type: `burnDamage: 4` on the Rust-moth and `tension: 100` on the Understudy. Every other special-case rule in BST-02 is behaviour and stays in `ai.js` / `combat.js`. | Both are numbers, and PLN-02 R1 keeps gameplay numbers in the spec and the data rather than hard-coded in a switch; M04's Burning tick and M08's Understudy script read them instead of special-casing a name. | M02 |
| D-020 | TEC-03, FLR-09, WLD-13 | `FloorDef` requires a `guard` string and types `boss.room` as the literal `'stairs'`, but floor 8 has no cache to guard and its boss stands on the map's `U` marker, not in a stairs room. | Floor 8 has `guard: null` and `boss: { type: 'The Understudy', room: 'fixed', … }`. `room` therefore has two values: `'stairs'` and `'fixed'`. | FLR-09 states floor 8 has no cache and no stairs, so there is nothing for either field to name; `null` and `'fixed'` say so explicitly rather than by omission, and the generator can branch on `room` instead of on the floor number. | M02 |
| D-021 | BST-04, BST-06, FLR-04, FLR-09, FLR-10 | FLR-10's nominal XP includes the Understudy's two summoned Unfinished but excludes the Conductor's up-to-four dancers, and neither summon list is in `FloorDef`. | The floor's `boss` carries `summonType` plus either `summonCount` (a fixed number, counted in the nominal XP — floor 8) or `summonCap` (a live cap, excluded — floor 3). | It lets the FLR-10 arithmetic be recomputed from data rather than restated as a constant, which is what M02's XP test is for, and M08's boss scripts need the same two numbers. | M02 |
| D-022 | FLR-09, SCR-07, TEC-03 | `FloorDef.journalPage` names the page found in the floor's journal room, but floor 8 has no journal room: page 8 is delivered by the ending sequence. | Floor 8 keeps `journalPage: 8`. | The field names which page belongs to the floor — SCR-03's header for page 8 is "Page 8 — The Escapement" — and the generator never places it because floor 8 has no journal role. | M02 |
| D-023 | SCR-01…SCR-07, UI-08 | `24` says "italics mark emphasis to render in `violet`" but the strings must be transcribed verbatim; JavaScript has no italics. | The `*asterisk*` markers are kept inside the strings; the text-box renderer (M10) treats a `*…*` span as violet. No other markup exists in `data/script.js`. | Dropping the markers would lose information the spec calls normative; inventing a parallel structure would stop the strings being verbatim. Asterisks are the spec's own notation. | M02 |
| D-024 | SCR-06, SCR-05, PLN-06 M02 | SCR-06's table wraps each of the Understudy's four lines in double quotes, but M02's test list requires `understudy[4] === 'Turn it, then. Someone has to.'` without them. | The four lines are stored without the outer quotation marks. Where a line is quoted inside prose — moment 3a — the quotes are part of that prose and are kept. | It is the reading the cited test passes under (PLN-07.1 step 1), and lines 2 and 3 are log lines, where a surrounding quote would be wrong on screen. | M02 |
| D-025 | SKL-01, SKL-02, SKL-03, SKL-04 | SKL-01 says a skill summary is "≤ 60 chars", but three of the twelve summaries written in SKL-02–04 are longer: Flywheel Guard 62, Resonant Pulse 77, Clockwork Decoy 79. | The summaries are transcribed verbatim and the 60-character rule is not asserted; the M02 test pins the twelve strings instead. UI-14's Skills screen must therefore wrap or fit a summary of up to 79 characters. | R1 makes the written content law and M02's whole job is verbatim transcription; rewriting three summaries to fit would change spec content, which R10 and the balance protocol both forbid outside their own procedures. Flagged for M10, which lays out the Skills screen. | M02 |
| D-026 | PLN-04, ACC-77, PLN-06 M02/M03 | M02 covers only ACC-77's static clause and M03 was to remove ACC-77 from the allowlist, but `acc-coverage` fails on any allowlisted ID that a test title already names — and M02's floor 8 map test names ACC-77. | ACC-77 is removed from the allowlist in M02, together with ACC-132. M03 still writes the loading half of ACC-77 (`loadFixedFloor`, the named positions) under the same ID. | The allowlist is a "not yet covered" list, not a "fully covered" list; an ID named by a passing test cannot stay on it. The M02 and M03 DoD lines were corrected to match (PLN-07.1 step 3). | M02 |
| D-027 | UI-07, BST-02, BST-06 | UI-07 says "Every enemy in `22` uses a distinct letter" and "bosses use uppercase", but The Unfinished (`u`) and The Understudy (`U`) are the same letter in different cases. | "Distinct letter" is read as "distinct glyph", compared case-sensitively. The two are kept as written. | The uppercase rule for bosses is the more specific one and BST-02/BST-06 fix both glyphs as numbers-in-a-table that R10 protects; on screen `u` and `U` are different glyphs in different colors (violet and white), which is what the rule is for. | M02 |
| D-028 | BST-05, ENM-06, BST-02 | The Regulator's table row gives a heavy attack (`3d5`) and no plain attack, but `EnemyType.attack` is not optional. | The Regulator gets `attack: '0 (flat)'`, exactly as the Gear-Golem — the other BRUISER-scripted type — is written in BST-02. | BST-02 already documents `0 (flat)` as "plain (never used, `ENM-06`)" for a bruiser, so this reuses the spec's own encoding rather than inventing an absent field. | M02 |
| D-029 | PLN-06 M02, TEC-03 | `data/floors.js` was written as a 0-indexed array (`FLOORS[n - 1]` is floor `n`), but PLN-06 M02 task 2 specifies `FLOORS[1..8]`, and every consumer (M03's `generateFloor(seedString, n)`, M04's ascend, M11's sim) works in floor numbers 1–8. | `FLOORS` is 1-indexed: `FLOORS[n]` is floor `n`, and index 0 is `null`. Iterate with `FLOORS.slice(1)`. | Matches the plan literally, and makes the likely mistake loud: a 0-based access returns `null` and throws at once instead of silently returning the wrong floor across nine later milestones. | M02 |
| D-030 | WLD-11 steps 5, 6, 8, 9; FLR-01 | WLD-11 says "a random interior tile" in six places but names a retry budget in only three. What fixes the draw order? | Where the spec states a retry budget (step 8's items, step 9's enemies, FLR-01's guard and boss) the draw is rejection sampling of `(x, y)` — x first, then y — repeated up to 50 times. Where it states none (stairs, station, journal page, cache tiles, pack members, step 5's journal room) a single draw indexes a candidate list built in reading order. | Rejection sampling is what "retry up to N times, then skip" literally describes; a single index into a pre-filtered list is the only way to honour a constraint ("adjacent to a wall", "outside the band", "within Chebyshev 2 of the first") without an unbounded loop, and it always consumes exactly one draw so the stream length stays fixed. | M03 |
| D-031 | WLD-11 step 4 | "Pick two distinct random rooms" — how is distinctness reached without an unbounded re-draw? | One draw over all rooms for the first, then one draw over the same list with the first removed. | Exactly two draws, always distinct, and the stream length does not depend on how often the same room came up twice. | M03 |
| D-032 | FLR-08, WLD-11 steps 6 and 10 | FLR-08 excludes only the band's own columns from feature placement, but then rejects the floor if any feature ends up within Chebyshev 1 of the band — so a placement the first rule allows can force a regeneration. | Feature placement on floor 7 excludes columns 27–32 (the band plus one tile either side). The Chebyshev-1 rule is still validated in step 10, where it catches the one feature that is not drawn at random: the start tile, which is its room's centre. | Both rules then hold by construction for the drawn features, which drops floor 7's WLD-10 retry rate to nearly zero and keeps generation inside TEC-14's 50 ms; the validation still fails the floors FLR-08 means it to fail. | M03 |
| D-033 | WLD-11 steps 6 and 9, FLR-01 | Step 6 lists the cache guard among the features, while step 9 and FLR-01 both place it after the spawn list. | The guard is placed in step 9: after every spawn-list entry and before the boss. | Two rules agree against one, and FLR-01 is the specific rule for guards; step 6's mention says which room receives the guard, not when it is drawn. | M03 |
| D-034 | WLD-12, ACC-72 | WLD-12 moves an enemy that is visible from the start tile with "retry 20 times, then accept", but ACC-72 asserts that *no* enemy is visible from the start tile. | After the 20 random retries, the first valid tile in reading order that is not visible from the start is used (no further draw). Only a floor with no such tile keeps a visible enemy. The cache guard is re-drawn inside the cache room and the boss inside the stairs room, so FLR-01's room assignment survives the fix. | PLN-07.1 step 1: the reading under which the cited ACC passes. "Then accept" still applies in the case it was written for — a floor with nowhere left to hide. | M03 |
| D-035 | WLD-07, WLD-11 steps 6 and 10, ACC-81 | Floor 1 places no Winding Station tile in the station room, yet step 10 requires that the station is "placed". | On floor 1 the start tile becomes the `&`, `stationSpent` is true, and no station-tile draw is made; step 5 still assigns the station role, as step 6 requires. Step 10's station clause is satisfied by that tile. | ACC-81 asks for exactly this floor 1 state, and WLD-07 says the floor 1 station "is the player start tile itself and is spent". Skipping the draw only shortens floor 1's own stream, which nothing compares against another floor's. | M03 |
| D-036 | ACC-71, WLD-07, WLD-12 | ACC-71 forbids an item on "a corridor/door/feature/hazard tile", but the journal page and the cache items are themselves items placed on tiles WLD-07 calls features. | "Feature tile" in ACC-71 means the terrain features — the player start, the up-stairs and the Winding Station. The journal page and the cache items are entries in `items[]`, and are additionally listed under `features` so that step 10's hazard-adjacency rule can see them. | It is the only reading in which ACC-71 is satisfiable at all, and it matches WLD-12's own list ("items only on room interior Floor tiles"), which the journal page and cache items obey. | M03 |
| D-037 | WLD-11 steps 7 and 10 | Step 7 keeps a hazard away from "the start tile, stairs, or station"; step 10 rejects the floor if any of five feature tiles (those three plus the journal page and the cache items) is within Chebyshev 1 of a hazard. | Placement keeps every hazard at Chebyshev ≥ 2 from all five feature tiles. | Step 10 is the rule with teeth: obeying the stricter list during placement removes a class of regenerations instead of discovering them one validation too late. | M03 |
| D-038 | ACC-70, FLR-07, WLD-07 | ACC-70 requires a cache of 2–3 items, while FLR-07 puts the Understudy Blueprint in floor 6's cache "on an additional tile". | The 2–3 count is the number of *rolled* cache items on every floor; floor 6's cache therefore holds 3–4 records in total, the Blueprint being the extra one. | "On an additional tile" makes the Blueprint extra rather than one of the rolls, and ACC-70's own parenthesis counts it separately ("floor 6: plus the Blueprint"). | M03 |
| D-039 | PLN-03, PLN-04, TEC-13 | PLN-03 fixes `createGame({seedString, rng})`, `act`, `state`, `phase`, `view()`, but the integration tier needs to start a run on a fixture map without the SCR-02 intro, and TEC-13 needs `loadFloor`/`loadFixture`. | `createGame` takes two further options — `floor` (a Floor object to enter instead of generating floor 1; the shape `gen.js` and `test/fixtures/maps.js` both produce) and `intro: false` (skip the intro text box) — and the returned game carries `drainEvents()`, `loadFloor(n)`, `loadFixture(floor)`, `derived()` and `ctx` alongside the PLN-03 five. | PLN-04 requires combat, AI, item and skill tests to run "on fixtures, not the generator, so they are exact", and TEC-13 already names `loadFloor` and `loadFixture` as debug hooks; both need an entry point that is not "generate floor 1". `intro: false` is the same need for `phase`: without it every fixture test would have to dismiss a text box first. None of the additions mutate state outside `act`/`loadFloor`/`loadFixture`, as PLN-07.3 requires. | M04 |
| D-040 | PLN-06 M04 task 2, ENM-06, CMB-03 | `turn.js` must call `ai.decide(enemy, state)` and execute what it returns, but no document defines the enemy action schema, and M06 must be able to replace `ai.js` in place. | The schema is fixed in M04 and documented at the top of `src/ai.js`: `wait`, `move{x,y}`, `melee{x,y}`, `heavy{x,y}`, `ranged{x,y}`, `openDoor{x,y}`, `breakDoor{x,y}`, `telegraph{flag,message?}`, `special{id,...}`. `decide(enemy, state, ctx)` receives the live instance and may mutate its own ENM-05 tracking fields; everything with a rule in `10-turns-and-combat.md` (damage, noise, statuses, death) stays in `combat.js`. `turn.js` throws on an unknown type. Tests may override the decision per enemy through the fixture's `ai` field, which `turn.js` prefers over the module. | It is the smallest vocabulary that covers every archetype and boss behaviour in ENM-06 / BST-04–06 while keeping every rule of `10` on the engine's side of the line, so M06 writes decisions only. The `ai` override is what PLN-06 M04 asks for ("the fixture accepts an `ai` override") and keeps the M04 energy-tier tests exact once the real AI lands. | M04 |
| D-041 | SCR-10 | SCR-10's article rule ("preceded by 'the', capitalised at the start of a sentence, unless the name itself begins with 'The'") must also work for templates that write the article themselves (`The {D} breaks.`). | `log.format` substitutes an actor label `{name, article}` by looking at the text immediately before the placeholder: a template that already ends in "the " / "The " gets the bare name, a sentence-initial placeholder gets "The ", anything else gets "the ", and a name that itself starts with "The " is never doubled. Tick and the Decoy pass `article: false` and are inserted verbatim. | It makes every SCR-10 row come out exactly as written without a second spelling of each template, and it is the only rule under which both `The {D} breaks.` and `{A} misses {D}.` are correct for the same label. | M04 |
| D-042 | TEC-05 | TEC-05 lists the state fields, but the turn loop needs a few more that are still plain JSON: where a run ended, what the panel reads for a cyclic hazard, and what the memory shows. | `state` additionally carries `dead` (`{cause, wound, floor}` or null) and `victory`; `state.floor` carries `memoryItems[]` (WLD-05's remembered items), `noises[]` (this turn's CMB-11 emissions, cleared at step 1, for ENM-05 in M06), `hazardCycle` (the derived UI-06 readout `{kind, active, warning, turnsUntilActive}` or null), `scrap[]`, `stationSpent` and `journalPage`; an actor carries `hazardTurn` (see D-046). All are JSON-serializable, so TEC-09 is unaffected. | TEC-05's list is what the state *contains*, not a closed set — it already ends `stats: { enemiesBroken, turns, ... }` and `flags: { …, scripted1, scripted2, ... }`. Every addition is either required by a rule with no other home (`noises`, `hazardTurn`) or is a cached derivation the UI is told to display (`hazardCycle`, `memoryItems`), recomputed after every action rather than maintained by hand. | M04 |
| D-043 | ITM-08, CMB-01, CMB-05, CMB-08 | The weapon slot holds either a melee or a ranged weapon, but CMB-01 gives Tick an `attack` (melee dice) at all times and CMB-08 needs a ranged weapon to fire. What is Tick's melee attack while a ranged weapon is equipped? | Unarmed `1d2`. `derive()` reports a melee weapon only when the equipped item's category is `melee`, and a `ranged` block only when it is `ranged`. Moving into an enemy with a Launcher equipped is therefore a `1d2` melee attack, not a refusal. | CMB-01 names `1d2` as the unarmed dice and CMB-05 makes a move into an enemy an attack unconditionally, so a refusal would contradict two rules; and the alternative (swinging the Launcher for its own dice) would let a ranged weapon deal its damage with no Tension cost. | M04 |
| D-044 | CHR-04, CAT-05, TEC-05, PLN-06 M04/M05 | `decayPeriod` is 5, or 6 with the Governor. Is it stored on `tick` or derived? | Derived: `derive(tick).decayPeriod` reads the equipped attachment's `REGULATED` special through `items.equipmentMods`, and `turn.js` asks for it fresh at every CMB-02 step 2. Only `decayCounter` is stored. | TEC-05 says derived values are recomputed and never stored, and PLN-06 M04 task 4 asks for it "derived so M05 can change it": M05 adds the Governor by writing one line in `items.js`, with no engine edit and no migration of saved state. | M04 |
| D-045 | CMB-08 | A throwable "lands on the last passable tile before the obstacle", but the projectile walk also stops on an *actor*, and CMB-08 centres the throwable's effect on that actor's tile. | `projectile()` returns both `tile` (where the flight stopped) and `landing`: the actor's own tile when it stopped on an actor, and the last passable tile walked when it stopped on a wall, closed door, chair or Escapement wheel. A throw uses `landing`; a ranged shot uses `actor`. | CMB-08 gives the two cases different words on purpose — "the actor or obstacle there is the actual target" versus "lands on the last passable tile before the obstacle" — and ACC-24 asserts the wall case exactly. Returning both keeps the Bresenham walk in one function for its two callers. | M04 |
| D-046 | WLD-08, CMB-02 steps 4 and 7 | A hazard lists **Enter** and **Standing** as two triggers. An actor that steps onto an active Steam Vent during step 1 is still standing on it at step 4 of the same turn: does it take the effect twice? | Once. `hazardHit` stamps `actor.hazardTurn` with the turn value it fired on, and the standing check at steps 4 and 7 skips an actor already stamped with the current turn. An ENTER check during step 1 uses the turn value the action will have after step 2 (the pre-increment `turn + 1`), exactly as WLD-08 states. | WLD-08 presents the two triggers as alternative ways into one effect ("**Standing** … while active, or **Enter** while active"), and the balance numbers of `31` count one vent hit per turn; charging 8 damage and two Burning applications for a single step would make floor 4 unplayable at the stated Integrity budget. | M04 |
| D-047 | PLN-04, ACC-117, PLN-06 M04/M10 | M04 covers only ACC-117's headless clause (the log line and the `death` event's `cause`, `header` and `flavor`), and the M04 DoD does not list ACC-117 among the IDs to remove — but `acc-coverage` fails on any allowlisted ID that a test title already names, and M04's two death tests name it. | ACC-117 is removed from the allowlist in M04. M10 still writes its browser clause (the Death screen as drawn) under the same ID. | The same reasoning and the same precedent as D-026: the allowlist is a "not yet covered" list, not a "fully covered" list, so an ID named by a passing test cannot stay on it. | M04 |
| D-048 | PLN-04, WLD-13, UI-07 | The fixture loader is specified as "the `WLD-13` legend plus `T` for Tick, letters for enemy types by glyph", but three WLD-13 terrain letters collide with a `22-bestiary.md` glyph: `C` (chair) is The Conductor, the chair's drawn glyph `h` is the Spring-Hound, and `U` (the Understudy's start tile) is The Understudy. | Letters are enemy glyphs; the colliding terrain moves. `test/fixtures/maps.js` accepts `@` and `T` for Tick's start tile, `E` and `O` for the Escapement wheel, and `H` for the chair. `U` and `1`–`9` need no ruling: on floor 8 those tiles *are* the actors WLD-13 marks, so the enemy reading is also the WLD-13 reading. | Placing actors is what a fixture map is for, and every combat, AI, item and skill test from M04 to M08 writes enemies by glyph, while the chair appears on exactly one map. `E` is WLD-13's own character for the Escapement wheel; `O` is its drawn glyph and was already in use. Only the test fixture is affected — `gen.js` still loads floor 8 from `data/floors.js` through the WLD-13 legend unchanged. | M04 |
| D-049 | SCR-10, ITM-06 | SCR-10's stack pickup template writes `{n}` twice — `Tick picks up {n} {X} (now {n}).` — but ITM-06's own example shows the two numbers differ: "Tick picks up 2 Solder (now 3)." One substitution value cannot fill both. | `items.js` fills the template's two `{n}` placeholders *positionally*: the first is how many were picked up, the second the resulting stack size. It reads the template through `log.template` and pushes the finished line, so `data/script.js` keeps SCR-10 verbatim and no template is invented. The single-item line stays `pickup` ("Tick picks up the {X}."); the stack line is used whenever the resulting stack holds more than the one item that was taken. | ITM-06 states the rendered text, which is what the player sees and what an acceptance test can read; SCR-10's placeholder spelling is a transcription artefact, and R10 forbids changing the rule while allowing the observable string to match the rule that defines it. | M05 |
| D-050 | ITM-03 | "Picking up a consumable adds to an existing non-full stack of the same name first; otherwise it takes a new slot." What happens when the pickup is larger than the room left in that stack (2 Solder onto a stack of 4)? | A stack is never split: if the whole pickup does not fit in one existing stack it takes a new slot, and if no slot is free the pickup is refused. `addToInventory` therefore only fills a stack when `count + existing <= 5`. | ITM-03 offers exactly two outcomes — one stack, or one new slot — and ACC-51 spells out the full-stack case as "New slot if free; else refused", with no third "partly here, partly there" state. Splitting would also make the ITM-06 log line ambiguous about which stack the "(now n)" counts. | M05 |
| D-051 | CAT-05, CMB-10, CMB-11, PLN-06 M05 | The Governor reaches `derive()` through `items.equipmentMods`, but the other two attachment specials have no hook: **Cooling** must stop a Burning that `combat.applyStatus` asks `actors.immuneTo` about, and **Quiet** must change the noise radius that `combat.attack` and the engine's door bump emit *before* anything in `items.js` is called. | Three one-line seams are added, each delegating to `items.js` so the rules still live in one module: `actors.immuneTo` asks `items.tickImmuneTo(tick, status)` for Tick (it previously returned a hard `false`); `combat.meleeAttack` passes `noise: items.meleeNoise(attacker)` for Tick's own melee, assigned *before* `opts`, so a skill that names its own noise (Overwind Strike's 6) still wins; `engine.js`'s door bump emits `items.doorNoise(tick)`. | PLN-06 M05 task 3 requires all three attachment specials, and ACC-61 is an M05 ID. Both effects are decided before `items.js` is reached on the call path — a woken enemy cannot be un-woken at CMB-06 step 6 — so no hook that fires later can implement them. The seams add no rule to the engine: each is a call into the module that owns ITM/CAT. | M05 |
| D-052 | ITM-07, SCR-10 | ITM-07 gives Equip a log line for each slot but names none for Unequip, and SCR-10 has no unequip template. | Unequip logs nothing on success; its refusal (no free inventory slot) logs SCR-10's `inventoryFull` line, which is the same condition ITM-03 gives that line for. | R1 and R10 forbid inventing a string that no spec writes, and SCR-10 is the closed list of log messages. The inventory screen shows the result immediately (UI-14), so nothing is hidden from the player. | M05 |
| D-053 | ITM-10, WLD-10, TEC-05 | `gen.js` rolls a floor's items against a *copy* of `state.uniquesGenerated` and never writes back, so nothing in M04 recorded that a unique had been generated. Where does the run learn it? | `items.placeFloorItems` records every `unique` among the records it places, and `items.onBreak` records a unique it drops. Both write `state.uniquesGenerated`, which `engine.js` already hands to `generateFloor` for the next floor. | Placement is the moment an item actually enters the run — a generation attempt that fails WLD-10 validation is discarded and must not burn a unique — and it is the one path every source of items (generator, cache, boss drop) passes through. It also keeps `gen.js` pure with respect to run state. | M05 |
| D-054 | CAT-01, TEC-07 | Sweep damages "every other enemy adjacent to Tick", and a Grit Bomb damages every actor in its area, but neither rule names the order the targets are resolved in. | Ascending `id` order, the same order CMB-03 resolves the enemy phase in (throwables use `combat.actorsWithin`, which is Tick first, then enemies by id). | TEC-07 requires the spec's stated order to fix the draw order wherever several actors are touched, and CMB-03's id order is the only ordering the specs give for "every enemy". Neither effect draws dice itself, but a break inside one does (ITM-11's `d100`), so the order is observable. | M05 |
| D-055 | ITM-07, ITM-11, WLD-02 | Drop is refused on "a feature tile" and ITM-11 names "(stairs, station)" — is that the whole list? | A feature tile is the up-stairs, the Winding Station, the chair and the Escapement wheel. The last two are not walkable, so in practice the rule bites on stairs and stations; hazard tiles are *not* features and accept a dropped item. | ITM-11's parenthetical lists the two an actor can stand on, and ITM-04 keeps items off "doors, features, or hazards" only for *generated* items while promising that "Items are never destroyed by hazards" — so a deliberate drop onto a hazard is safe and legal. | M05 |
| D-056 | ITM-10, ITM-11 | A unique is re-rolled "until it does not" duplicate, on the assurance that "uniques are always in tables that contain at least one non-unique entry" — but both boss drop tables in `22-bestiary.md` hold exactly one entry, and it is the unique. | `rollTable` only enters the re-roll loop while the table still holds an entry that is not an already-generated unique; otherwise it keeps the roll. A boss's one-line table therefore returns its drop rather than spinning forever. | ITM-11 states the intent directly — "their drops are unique … a unique item is never lost" — and the loop as literally written would not terminate on the data the spec itself ships. The situation cannot arise in a real run (each boss appears once), so the guard only protects against a hang. | M05 |
| D-057 | BST-02, SCR-10, PLN-06 M06 | BST-02 writes two Gear-Golem log lines ("The Gear-Golem raises its arm." / "…brings its arm down.") that SCR-10's template table does not list, and `data/script.js` was transcribed from SCR-10 alone. | The two strings are added to `data/script.js` verbatim as `golemTelegraph` and `golemHeavy`, and `ai.js` names them through its `FLAVOR` table (the same shape the Cuckoo and Archivist lines already use). | R1 makes BST-02's written content law and ENM-11 requires every non-plain attack to carry a telegraph line, so the lines must exist; transcribing them keeps D-052's rule that `ai.js` never invents a string, and SCR-10's table is a list of the *shared* templates, not a closed set (it already omits the boss lines of BST-04–06). | M06 |
| D-058 | ENM-06 GUARD, TEC-03, PLN-04 | A GUARD's "in bounds" is defined against `homeRoom`'s interior, but a spawn record may carry no room at all — every fixture map has `rooms: []`, and `createEnemy` defaults `homeRoom` to `-1`. | `ai.homeRoomOf` falls back to the 1x1 rectangle of the guard's own `homeTile`, so "in bounds" becomes "within Chebyshev 2 of the tile it guards". | It is the degenerate case of the same rule rather than a second rule, it keeps ENM-06's GUARD list total (no archetype may throw mid-phase), and it is the reading under which a guard with no room still guards something. Generated floors always name a room (FLR-01), so this only ever fires on fixtures and hand-built spawns. | M06 |
| D-059 | ENM-08, WLD-08 | ENM-08 says cyclic hazards are impassable "if they would be active on the enemy's arrival turn, i.e. next turn"; WLD-08 says enemies path around hazards "that are active or would be active on their arrival turn". One names only the next turn, the other names both. | A cyclic hazard tile is impassable when it is active on `state.turn` **or** on `state.turn + 1`. Grinding Gears are CONSTANT and therefore always impassable on the first plan; when that plan finds no path, `pathTo` re-plans once with them passable, exactly as ENM-08's second clause says. | It is the only reading that satisfies both sentences at once, and it is the conservative one: an enemy that steps onto a vent the turn it fires would take the WLD-08 ENTER hit at `state.turn`, which is the case ENM-08's "arrival turn" wording is trying to avoid. | M06 |
| D-060 | ENM-05, CMB-11 | "Any noise event during the previous player turn within its `perception + 3` sets `lastKnown` to the noise tile" — several noises can land in one turn, and the rule does not say which wins. | `state.floor.noises` is walked in emission order and every entry within reach overwrites `lastKnown`, so the most recent qualifying noise is the one the enemy walks toward. `lastKnownAge` is reset to 0 by any of them. | "Sets lastKnown" applied to each event in order is the literal reading, and the last event is the freshest information — the same tie-break CMB-11 implies for waking, where later noises simply re-target a Dormant enemy that has already woken. | M06 |
| D-061 | ENM-06 SKIRMISHER line 2, ENM-08 | The Retreat step is described as "move to the neighboring tile that maximizes Chebyshev distance" without saying which neighbours are eligible. | The candidates are the tiles of the 8-neighborhood that are walkable, hold no actor, and are not a diagonal into or out of a door (ENM-08's last bullet); hazard avoidance, which ENM-08 states for *pathing*, is not applied to a retreat. | A retreat is a one-tile move, so the move rules of ENM-08 apply and its path rules do not; without the walkable/unoccupied filter the action would be executed as a no-op by `turn.js` and the Skirmisher would silently lose its turn, which contradicts "If no neighboring tile increases distance -> Melee Attack". | M06 |
| D-062 | PLN-04, PLN-06 M04/M06, ENM-06 | Three M04/M05 tests (ACC-19, ACC-79 and the WLD-08 enemy-hazard unit test) placed an enemy in Tick's line of sight with no fixture `ai` override, and passed only because the M04 `ai.js` stub always Waited. Replacing the stub made those enemies wake and attack. | The three fixtures now carry the documented `ai: aiWait` override from `test/fixtures/maps.js`. No assertion changed and no source rule changed. | `test/fixtures/maps.js` states the mechanism ("a test that needs an enemy to act supplies its own decision function … which keeps the M04 tests exact once M06 lands"); an enemy that must *not* act needs the same override. The alternative — weakening ENM-06 so enemies do not chase — would contradict R1. | M06 |
| D-063 | ENM-04, WLD-08, CMB-11 | ENM-04 rule 3 says a damage wake sets `lastKnown` to Tick's tile "if Tick is within 10 tiles, else the damage source tile", but `combat.damage` preferred an `opts.wakeTo` source unconditionally and fell back to Tick's tile — and no caller ever passed `wakeTo`, so the "else" branch was unreachable and a far wake still pointed at Tick. | `damage` applies the rule as written: within Chebyshev 10 of Tick the wake points at Tick's tile, beyond it at `opts.wakeTo` when the caller named a source (Tick's tile when none did). `combat.hazardHit` passes the hazard tile, which is the one damage source that can sit more than 10 tiles from Tick on floors 4 and 7. | The clause only bites when damage reaches an enemy far from Tick — a hazard now, Sympathetic Break in M07 — and without a reachable else-branch that enemy would walk toward Tick instead of toward what hit it. | M06 |
| D-064 | CHR-07, CHR-09, CMB-05, TEC-09, PLN-03 | PLN-03 lists `{type:'takeSkill', name}` among the engine actions, but CMB-05's action table never says whether taking a skill costs a turn. | It costs a skill point and no turn: `engine.act` resolves `takeSkill` before the CMB-02 loop, so steps 2-10 do not run, and it is accepted while Tick is Stunned like any other screen action. Nothing is logged on success (SCR-10 has no line for it). `resolveAction`'s own `takeSkill` case is therefore unreachable and returns `reason:'freeAction'`. | CHR-07 opens the Skills screen *inside* step 8 and calls it "a free interruption; the loop is already complete", so spending a point there cannot start a second turn; TEC-09 lists "each turn-costing action, ascend, `takeSkill`" as three separate autosave triggers, which only reads if `takeSkill` is not turn-costing; and CHR-09 names the cost as "exactly 1 skill point" and nothing else. D-052's rule keeps a string that no spec writes out of the log. | M07 |
| D-065 | PLN-03, CHR-10, UI-10 | PLN-03's action is `{type:'skill', slot, x?, y?, dx?, dy?}` but does not say whether `slot` is the UI-10 hotkey (1-4) or an index into `activeSlots`. | `slot` is the hotkey, 1-based: `activeSlots[slot - 1]`. A `name` field is also accepted and wins over `slot`, so a caller that knows the skill (a test, a bot, the Skills screen) need not compute the slot. Clockwork Decoy accepts either `{x, y}` or `{dx, dy}` for its adjacent tile. | UI-10 binds the keys `1`-`4` and CHR-10 says the slots *are* those hotkeys, so 1-based is the only spelling in which `slot` means what the player pressed. `name` adds no rule: it names the same twelve skills. | M07 |
| D-066 | SKL-02, CMB-02 steps 1 and 3, CMB-10 | Flywheel Guard lasts "for 4 turns", but a self-buff applied in step 1 is decremented by the very next status tick (step 3 of the same turn), which would leave only three enemy phases protected. | `guardTimer` is set to 5 on use (and on a re-use "resets the timer to 4" the same way), so after that turn's step 3 it reads 4 and the Guard covers the enemy phases of the turn it was used in and the three after it — four turns. | CMB-10's counting rule fixes a duration as the number of *phases* an effect covers ("Stunned N on an enemy costs it exactly N enemy phases") by giving a status applied in step 1 its first decrement after the enemy phase; Tick's own status tick runs before the phase instead, so the equivalent guarantee needs the extra turn. The panel still shows `Gua(4)` down to `Gua(1)` because it is read after step 3. | M07 |
| D-067 | SKL-03, CMB-02 step 6, ACC-40 | The Decoy retargets "on placement and at the start of each enemy phase for 6 turns" and is removed "after 6 turns" — two clauses that cannot both be satisfied by one decrement if the removal happens before the sixth phase's enemies act. | The upkeep runs at the start of the enemy phase: it retargets, then decrements the timer once per turn (so a Slowed Tick's double phase, CMB-04, does not cost two turns). The removal is noticed at the start of the *next* phase after the timer reaches 0, so the Decoy holds enemies for the placement turn's phase and the five after it — six phases — and vanishes on the sixth turn after placement. | It is the reading under which ACC-40's two clauses both pass: enemies target it for six enemy phases *and* it is gone once six turns have elapsed. Removing it at the head of the sixth phase instead would decoy only five. | M07 |
| D-068 | SKL-04, TEC-07, ACC-42 | Resonant Pulse says "every enemy within Chebyshev 2 of Tick takes `1d4+2` damage": one roll shared by every enemy, or one roll each? | One roll per enemy, drawn in ascending `id` — the order SKL-04 itself states for the effect. A break resolves fully (its ITM-11 drop roll and any Sympathetic Break chain) before the next enemy is damaged. | "Every enemy takes `1d4+2`" is a per-enemy effect, the same shape as an attack's damage roll, and TEC-07 requires the spec's stated order to fix the draw order wherever several enemies are touched (the precedent is D-054 for Sweep). | M07 |
| D-069 | SKL-05, SKL-03, SKL-04 | "**Sympathetic Break + Salvage:** both trigger on the same break; Salvage counts chained breaks" does not say which of the two runs first. | Salvage first, then Sympathetic Break, inside one `onEnemyBroken` hook. The counter therefore advances in the order the breaks actually happen, and a chain of four breaks drops its consumable on the fourth enemy rather than on the first. | Both orders give ACC-44 the same total of 3, so the tie-break is the reading that is simplest to explain (PLN-07.1 step 1): "every fourth enemy broken" counted in break order. Running the chain first would count the *last* enemy of a chain before the enemy that started it. | M07 |
| D-070 | PLN-06 M07, SKL-02, SKL-03, CMB-03, CMB-12, ENM-04 | Three of the twelve skills need a seam the engine did not already have: Piston Drive tests every melee hit Tick makes, the Decoy must retarget "at the start of each enemy phase", and a Decoy at 0 Integrity reaches `combat.breakActor`, which is written for enemies. | Three one-line seams, each delegating to `skills.js` so the rules stay in one module: `combat.meleeAttack` passes its result to `skills.onTickMeleeHit` for Tick's own attacks; `turn.js#enemyPhase` calls the new `ctx.hooks.onEnemyPhase` before anything else in the phase; and `combat.breakActor` returns through `skills.removeDecoy` when the broken actor `isDecoy` (so there is no scrap, XP, drop roll or noise). `combat.damage` additionally skips ENM-04's wake for the Decoy, which has no AI state. | The pattern and the precedent are D-051's three seams for the attachment specials: the decisions all happen on a call path that is inside `combat.js`/`turn.js` before `skills.js` could otherwise be reached, and each seam adds no rule to the engine. PLN-06 M07 already names the first of the three ("Piston Drive (threshold hook in `combat.meleeAttack`)"). | M07 |
| D-071 | PLN-03, SCR-07, UI-17, UI-19 | PLN-03 lists `endingChoice` among the `textbox` ids *and* gives the ending choice its own `{type:'choice'}` event, so SCR-07's setup text could arrive as a text box that the player dismisses before a second, wordless choice screen. | One event. `{type:'choice', id:'endingChoice', text, options}` carries SCR-07's three paragraphs and its two option labels, and no `textbox` with id `endingChoice` is ever emitted; `endingChoice` is the choice event's `id` instead. | UI-19's row shows one step ("Ending choice") between thought 3 and page 8, and UI-17 draws a choice as a single centred panel with its prompt above the options; a dismissible box in front of it would add a screen the transition table does not have. Keeping the id satisfies PLN-03's list without emitting a second event. | M08 |
| D-072 | BST-04, BST-05, BST-06, SCR-10, R1 | The twelve boss log lines ("The Conductor raises the baton.", the two Regulator BRUISER lines, "The Understudy hums.", …) are written in the phase lists of `22-bestiary.md`, not in SCR-10's template table, so `data/script.js` had no home for them. | They are transcribed verbatim into `data/script.js`'s `log` table under `conductor*`, `regulator*` and `understudy*` keys, and `bosses.js` prints them through `log.say` like any other line. The Regulator's two BRUISER lines additionally go into `ai.js`'s `FLAVOR` table, because it is the ENM-06 BRUISER script that prints them. | The precedent is D-057, which did exactly this for the Gear-Golem's two BRUISER lines from BST-02; R1 makes the bestiary's spelling law and R2's data rule keeps every string out of `src/`. | M08 |
| D-073 | BST-03, CMB-08, PLN-06 M08 | "Phase transitions happen at the moment Integrity crosses the threshold" needs a seam inside `combat.damage`, and BST-03 does not say what a single hit that crosses *two* thresholds does, nor what a hit that both crosses a threshold and breaks the boss does. | `combat.damage` calls the new `ctx.hooks.onBossDamaged` right after it has applied the damage and ENM-04's wake, and `bosses.onDamaged` walks the phases one at a time up to the phase the new Integrity puts the boss in — so a hit from Phase 1 to Phase 3 runs both transitions, in order, printing both log lines and applying both sets of side effects. A hit that leaves Integrity at or below 0 runs none: the boss is defeated, not transitioning. | "At the moment Integrity crosses the threshold" is true of each threshold the hit crosses, and BST-06's Phase 2 transition (line 2 plus two summons) is a one-off event that a Phase 3 hit must not silently skip. A broken boss has no "next action" for the new phase's script to apply from, and BST-06 makes defeat its own sequence. | M08 |
| D-074 | BST-06, CMB-03 | "Every action it takes costs 2. If it reaches 0, the Understudy is defeated exactly as if broken" does not say whether the action that empties the spring is taken first. | The Understudy pays before it acts: 2 is deducted at the head of its decision, and if that leaves the spring at 0 it is defeated on the spot and takes no action (not even a Wait). Its action counter `n` is not incremented by the action it never took. | It is the reading in which the spring is a cost rather than an after-the-fact check, which is how Tick's own Tension works (CHR-04: the spring drops and *then* the run ends at 0), and it makes the boss's last action the one that leaves the spring at 2 — so the number the inspect popup shows is always a spring it could still spend. | M08 |
| D-075 | BST-05, BST-06, SKL-04 | Vent hits "every actor within Chebyshev 2 of the Regulator" and Pulse "every actor within Chebyshev 2" — a set that literally contains the boss itself, at distance 0. | The boss is not one of its own victims: both specials skip the actor that used them and hit every other actor in the radius (Tick, summons, and any other enemy). | Resonant Pulse (SKL-04) is the same shape of effect and excludes its user by naming "every enemy within Chebyshev 2 of Tick"; a Regulator that Vented itself would also be knocking itself back with Pulse under BST-06, which no phase list mentions. Both bosses are immune to the status their own special applies (Burning for the Regulator) in any case, so only the damage would ever have landed. | M08 |
| D-076 | STY-08, SCR-08, PLN-06 M08 task 5 | STY-08's summary field is "floors reached" and SCR-08 labels it `Floor reached`, but the engine reported `state.floorNumber` — the floor Tick is standing on, which is not the same number once anything can put Tick on a lower floor. | The summary reports `state.stats.floorsReached`, the highest floor number entered this run. Both readings agree in normal play (Tick only ascends), so no acceptance test changes. | "Floors reached" is a high-water mark by name, and `state.stats` already keeps it for exactly this screen (TEC-05). | M08 |
| D-077 | BST-06, SKL-03, SKL-05, ITM-11 | BST-06's "On defeat: no scrap, no XP" does not say whether the Understudy's break still counts for SKL-03 Salvage and SKL-04 Sympathetic Break, both of which fire on `onEnemyBroken`. | It counts. `combat.breakActor` runs unchanged — the ITM-11 drop roll is drawn, `stats.enemiesBroken` advances, Salvage's counter advances and Sympathetic Break may chain — and `story.onEnemyBroken` then removes the scrap pile from the Understudy's tile. XP needs no special case: `data/enemies.js` gives the Understudy `xp: 0` per BST-04..06's table. | BST-06 withholds two specific rewards and says nothing about the skills, and SKL-03 counts "every fourth enemy broken" without exceptions; taking the scrap back after the break keeps the one boss-specific rule in `story.js` instead of adding a boss branch to `combat.breakActor`. The drop roll still being consumed is what ITM-11 requires of every break and is what keeps a scripted RNG queue predictable (R6). | M08 |
| D-078 | PLN-04, PLN-06 M08, BST-05, BST-06 | Two M08 boss tests read the `ventingUp` / `pulsingUp` wind-up flags after a five-turn loop, where the special that consumes the flag has already cleared it — so the same flag was asserted both true and false at the same instant. | The tests snapshot the flags after each turn and assert on the snapshot. No source rule changed: `bosses.js` raises the flag on the telegraph action and clears it on the special exactly as BST-05 line 2 / BST-06 line 2 write it. | R6 makes a test a statement about one turn's state, and a transient flag can only be observed on the turn it is raised; the precedent for fixing the test rather than the rule is D-062. | M08 |
| D-079 | PLN-02 R2, TEC-02, D-001, PLN-06 M09 | R2 and TEC-02 both exempt "the storage adapter in `save.js`", and `dom-free.test.js` permits the single identifier `localStorage` there (D-001). Does `save.js` have to use that exemption? | No. `createStore(adapter)` takes the storage it should write through, so `src/save.js` names no DOM identifier at all: the Node tests hand it a `Map` and M10 hands it `localStorage`. The adapter is duck-typed and accepts either `getItem`/`setItem`/`removeItem` or `get`/`set`/`delete`, so neither call site needs a wrapper. D-001's permission stays in place, unused. | PLN-06 M09 requires every TEC-09 rule to be provable headlessly ("Adapter injected so Node tests use a Map"), and an injected adapter is the only way the *engine's own* autosave and delete paths are exercised by a Node test rather than only by M10's browser specs. It also leaves R2 with nothing to forgive. | M09 |
| D-080 | TEC-05, TEC-09, TEC-15, D-040 | TEC-09 stores the state with a plain `JSON.stringify`, which silently turns a `Set` into `{}`, drops a function or an `undefined` property, and writes `NaN` as `null`. What happens when a later milestone puts such a value in the state? | `serialize` walks the state first and throws on anything a `JSON.parse(JSON.stringify(...))` round trip would not return unchanged: a function, a `Set`, a `Map`, a typed array, any non-plain object, a non-finite number, an `undefined` property, and a cycle or shared reference. The single documented exception is an enemy's `ai`, the test-only per-enemy decision function of `test/fixtures/maps.js` (D-040), which is dropped. | TEC-15 makes the code assert its own invariants, and "the whole state survives a save" is the invariant M09 exists for. A loud failure on the first autosave is a bug found in the milestone that added the field; a silent drop is a bug found by a player whose reload lost a scripted moment. The `ai` exception is the one non-JSON value that legitimately exists, and only in Node fixtures. | M09 |
| D-081 | TEC-09, TEC-05, PLN-06 M09 task 1 | M09 task 1 asks `deserialize` to "validate version and shape, rebuilding `Set`s and derived values", but TEC-09 also says Continue "restores `state` verbatim" and never migrates. What is there to rebuild, and what does a malformed save do? | Nothing is rebuilt: the state M04-M08 keep holds no `Set` and no derived value (the M09 round-trip test proves it over three different runs), so `deserialize` only validates and returns the parsed object as it stands. `engine.js`'s restore path recomputes `derive()` and the FOV, which is where TEC-09 puts them. A save that fails the shape check joins TEC-09's existing branch: treated as no save, and deleted by `store.load()`. | TEC-05 already promises derived values are "recomputed, never stored", so a rebuild step would have nothing to do and would be the only place in the codebase that could quietly repair a save. A truncated or hand-edited save is no more resumable than a version 0 one, and TEC-09's "never attempt migration" is the rule for both. | M09 |
| D-082 | PLN-03, TEC-09, D-039 | TEC-09's Continue needs an engine built from a saved state, but PLN-03's `createGame` only knows how to start a run, and D-039 added `floor`/`intro` for fixtures. | Two further options, in the same spirit: `state` (a deserialized TEC-05 state, adopted verbatim - no floor is generated, no intro is emitted, the play RNG is rewound to `playRngState`, the FOV and the panel's derived values are recomputed, and the log gets "Tick resumes.") and `store` (a `save.js` store the engine writes and deletes on TEC-09's triggers). `game.autosave()` and `game.deleteSave()` are exposed for the two triggers that are not engine actions - Pause leaving to the title (UI-18) and the UI-17 Abandon prompt. | "Restore into a fresh engine" is what PLN-06 M09's ACC-02 test asks for, and adopting the saved object as `state` is the only way `ctx.lines`, `ctx.state` and `game.state` keep pointing at one object as TEC-05 requires. Passing the store in rather than importing it keeps `engine.js` free of any storage decision, so R2 needs no exemption there either. | M09 |
| D-083 | TEC-09, CMB-02 step 9, UI-16, UI-19, SCR-07, ACC-03, ACC-115 | TEC-09 lists when the save is written and deleted, but a turn can end with a blocking text box or the ending choice queued - and those events live outside `state`, so a save taken there restores into a run that can never finish the sequence. TEC-09 also does not say whether starting a new run writes a save before the first turn. | `autosave()` branches on `engine.phase`. `ended` (dead or victorious) deletes the save, which is TEC-09's death and victory rule. Any other non-`run` phase - a queued `textbox`, `journal` or `descent`, or a pending `choice` - writes nothing and leaves the previous save alone. Only phase `run` writes. Nothing is written when a run is created; the first turn-costing action, or Pause leaving to the title, writes the first save. | It is the reading that makes ACC-115 reachable rather than merely usually reachable: BST-06's defeat sequence marks `moment3` and queues three events inside the turn that broke the Understudy, so a save written at step 9 of that turn would restore a run standing next to a dead boss with the ending already "seen". Rewinding to the previous save costs the player one turn and keeps every ending reachable. TEC-09's trigger list is explicit and does not include "on new run", and UI-18's "Quit to title" already covers the run a player starts and immediately leaves. | M09 |
| D-084 | ACC-02, TEC-09, UI-04 | ACC-02 requires a restored run's "log tail" to be identical to the uninterrupted run's, but TEC-09 makes Continue print "Tick resumes." - so the two logs can never be equal line for line, and the extra line can also break a UI-04 merge that would otherwise have happened across the restore point. | The ACC-02 comparison expands every line by its UI-04 merge `count` into the list of messages the player actually read, removes the restore line from the restored run, and compares those lists. Everything else about the two states is compared with a strict deep equality that excludes only `log`. | It is the only comparison under which ACC-02's clause is both satisfiable and strict: it is insensitive to *where* a merge boundary fell (which is what the inserted line changes) and to nothing else, so a genuinely different log still fails. The restore line itself is asserted separately - present exactly once, at the end of the pre-restore log. | M09 |
| D-085 | ENM-03, CMB-02 step 9, TEC-09, ACC-02, ACC-14 | `turn.js` cleared ENM-03's `wokeThisTurn` flag after the CMB-02 step 9 autosave hook, so a save written on the turn an enemy woke restored with the flag still raised - and CMB-03 step 1 would deny that enemy its energy grant a second time. | The flag is cleared immediately before the step 9 hook instead. No rule changed: the flag is only read inside the same turn's enemy phase (CMB-03 step 1), so clearing it a few lines earlier is invisible to a run that is never interrupted. | ACC-02 requires a restored run to be indistinguishable from one that was never reloaded, and "woke this turn" is per-turn bookkeeping, not state a turn boundary should carry. A saved state has to be one the turn loop can start a *new* turn from, which is what step 9 being the last mutation of the turn means. | M09 |
| D-086 | SCR-09, UI-10, UI-12, UI-14, TEC-02 | SCR-09 says the Help screen's key list is "generated from `UI-10`", but `24-script.md` (and therefore `data/script.js`) holds no key names. Where do the key rows live? | The SCR-09 rule lines come from `data/script.js` verbatim (`SCRIPT.help`); the key rows are transcribed from the UI-10 / UI-12 / UI-14 tables into `src/screens/help.js`. The same split applies to the other UI-only literals — UI-03's panel labels, UI-14's button row, UI-16's `— any key —` / `— more —` — which the screens carry directly. | TEC-02 defines `data/script.js` as document `24`, and the key tables are document `15`; putting them in `data/` would make that module a transcription of two specs, while a `src/screens/*` table keeps `data/` matching TEC-02. Every string that `24` *does* write is still read from `data/script.js`, so no scripted text is duplicated. | M10 |
| D-087 | UI-16, ACC-114 | UI-16 says a text box "scrolls with arrows/wheel" *and* is "Dismissed by any key or click". An arrow key is both. | The UI-16 scroll keys (arrows, numpad 8/2, PageUp/PageDown, Home, End) and the wheel scroll and do not dismiss; every other key, and any click, dismisses. | It is the only reading in which both sentences do something: if an arrow dismissed the box, the scrolling rule would be unreachable, and `— more —` could never be read to the end. ACC-114's "dismiss with any key or click" is still satisfied for every key a player would press to move on. | M10 |
| D-088 | SCR-07, UI-16, ACC-116 | Ending B's descent is "seven lines, one at a time, each held 1.5 seconds (skippable)", which does not say whether a key skips one line or the whole sequence. | Any key or click ends the whole descent and moves on to the Ending B text. | "Skippable" applied to the sequence is what ACC-116's parenthesis describes ("seven descent lines 1.5 s each (skippable)"), and a per-line skip would be nearly indistinguishable from waiting, since the lines are shown one at a time in any case. | M10 |
| D-089 | UI-17, SCR-08, ACC-118 | UI-17 writes the summary's last two rows as `Journal pages: n/8` and `Seed: xxxx`; SCR-08's label list writes them as `Journal pages {n}/8` and `Seed {seed}`, without the colons. | SCR-08's spelling, taken verbatim from `data/script.js`'s `summaryLabels`. | SCR-08 is the document that fixes the strings ("Summary labels (two columns)"), and ACC-118 asserts the rendered text is `Seed abc` — which only holds without the colon. UI-17's prose is describing the row, not quoting it. | M10 |
| D-090 | UI-10, UI-05, SCR-10, D-052 | UI-10's `c` waits for a direction and `t` waits for "a throwable from a letter list", but no document writes the prompt either mode shows, nor the refusal when `t` has nothing to throw or `f` has no ranged weapon — and SCR-10's table is the closed list of log messages. | All four strings are UI text on the inspect line (UI-05's row 24), never log lines: `Close which door? (direction, Esc cancels)` while `c` is pending, `Throw which? (letter, Esc cancels)` while the throw list is open (the list itself is a box over the map), and `Nothing to throw.` / `No ranged weapon equipped.` for the two refusals, each cleared by the next input. Nothing is written to the log, and no turn is spent. | D-052's rule keeps invented strings out of the log, and UI-05 already gives row 24 to this class of text by giving it to the look cursor. A free, immediately cancellable prompt or refusal is exactly what the inspect line is for; putting it in the log would leave it in the Message History for the rest of the run, and SCR-10's `Nothing to shoot.` means something else (CMB-08: the shot had no target). | M10 |
| D-091 | TEC-10, TEC-13, ACC-100, PLN-07.3 | ACC-100 asserts the canvas sizing of TEC-10 — cell aspect 0.6, the largest integer cell, the grid fully visible and centered, letterboxing — but none of it leaves a trace in the 80 × 30 cell buffer that `CH.grid()` returns, and `CH.state` is gameplay only. | A read-only hook `CH.metrics()` was added, returning the current cell metrics (`cell`, `cellW`, `cellH`, `gridW`, `gridH`, `originX`, `originY`, `width`, `height`, `cols`, `rows`, `aspect`, `integer`) plus the backing store's `canvasWidth` / `canvasHeight`. It is documented in TEC-13 in the same change and mutates nothing. | PLN-07.3's procedure exactly: the narrowest read-only hook for a UI rule that `CH.grid()` / `CH.state` cannot show. Recomputing the metrics inside the test instead would assert the spec against itself rather than against what the renderer did. | M10 |
| D-092 | UI-14, ITM-07, ACC-109 | UI-14 lists four inventory actions that cost a turn (ITM-07) but says only of Use, through ACC-109, that the screen closes. What do Equip, Drop and Throw do to the screen? | `u` and `t` close the Inventory — the item leaves the list, and Throw continues into targeting (UI-12). `e` and `d` leave it open, so several slots can be handled in one visit. | ACC-109 fixes the Use case, and Throw cannot stay open because targeting needs the map. Equip and Drop have no such reason, and UI-14's own mock-up shows the equipped rows beside the inventory rows, which is only useful if the screen survives an equip. | M10 |
| D-093 | UI-15, SKL-01, D-025 | UI-15 lays the Skills screen out as "three columns, one per discipline" with "the selected skill's full text (`20`) shows in a box below the columns", but three of the twelve summaries are 62, 77 and 79 characters (D-025) and no 20-cell column can hold one. | The Skills screen is full width (80 cells): three 26-cell columns hold the rank lines (`n. Name`, which all fit), and the detail box below them spans the whole grid and **word-wraps** the summary and the description. Nothing is truncated — no `…` appears on the screen. | D-025 flagged this for M10, and PLN-07.1 step 1 prefers the reading the cited test passes under: ACC-110 and the SKL-01 text are both satisfied only if the full summary is readable. UI-15 constrains the *columns*, not the detail box, so widening the box changes no rule. | M10 |
| D-094 | SCR-09, UI-16, ACC-120 | Three of SCR-09's six rule lines are 81, 89 and 90 characters, longer than UI-01's 80-column grid, and ACC-120 requires them "verbatim". | The Help screen word-wraps each rule line at 76 columns and indents the continuation by one space. No line is truncated, and ACC-120 compares the screen's text with whitespace collapsed, so the wrap is invisible to the assertion. | A `…` would break "verbatim", and shortening the lines would change SCR-09's content, which R1 and R10 forbid. Wrapping is the mechanism TEC-10 already fixes for every other long string in the game. | M10 |
| D-095 | TEC-13, PLN-04, TEC-02 | TEC-13's `CH.loadFixture(rows, opts)` must build a floor from a `test/fixtures/maps.js` ASCII map, but that module lives under `test/`, and nothing under `src/` may depend on the test tier. | `main.js` imports the fixture loader with a dynamic `import()` inside the hook, so `src/` carries no static dependency on `test/`. `CH.loadFixture` therefore returns a promise; it also accepts an already-built Floor object, in which case nothing is imported. | It is the only way to satisfy both TEC-13 (which names the hook and its ASCII-map argument) and TEC-02's file layout, without a build step or a copy of the loader inside `src/`. `tools/serve.js` already serves the repo root "so `test/fixtures/` is importable from e2e tests" (PLN-06 M00), which is the same requirement one level down. A browser test runner awaits a returned promise, so the signature change is invisible to the tests. | M10 |
| D-096 | PLN-03, UI-16, UI-19, SCR-07 | PLN-03 gates the turn loop with `phase`, so a text box is expected to correspond to an `awaitDismiss` the UI answers with `dismiss`. But the ending sequence emits `journal`, `descent`, `textbox` and `victory` in one action, and `state.victory` has already put the engine in `ended` when they are handed over — so there is no `awaitDismiss` to step through, and reading `phase` would show one box and lose three. | `main.js` keeps its own FIFO of engine events and shows exactly one overlay per event, in order, taking the next event only when the current overlay has been answered. It acts `dismiss` only while `phase === 'awaitDismiss'`; in phase `ended` it simply walks its own queue. | It is the reading in which UI-19's Understudy row happens as written (text box → text box → Ending choice → page 8 → descent → ending text → Victory), and it is also what the intro and the STY-05 moments need, where `phase` *is* `awaitDismiss`. The queue is the UI's own state, so no engine rule changes; `CH.events()` returns it (TEC-13). | M10 |
| D-097 | BAL-04, BAL-C4, OVR-05 | BAL-04's "Largest possible single hit" column names one threat per floor (floor 5 "Cuckoo 4", floor 7 "Knight 8"), but the arithmetic maximum over each floor's whole BST-02 roster is larger — the Brass Finch's `1d4+1` is 5 on floor 5 and the Gear-Golem's `3d4` is 12 on floor 7. Which does BAL-C4 check? | `balance.test.js` recomputes the true maximum over every enemy whose `floors` list contains the floor (melee, heavy and ranged dice), every WLD-08 hazard on it, and BST-05/BST-06's specials, and asserts the OVR-05 40% cap against that. The measured column is 3, 5, 5, 12, 5, 15, 12, 16. | The stricter reading: BAL-C4 is "all ratios <= 40%", and testing the larger number tests the rule. The documented entries are the *notable* threat a player must plan around, not the maximum; both readings pass, the worst ratio being the Regulator's 15 of 60 on floor 6 (25%), exactly as BAL-C4 states. No spec number is changed (R10). | M11 |
| D-098 | BAL-07 S2, CHR-03, CHR-05, BAL-01 | BAL-07 spells out "never uses ... stations" for S3 but says nothing about stations for S2, and the target (arrives at floor 8 with 60-80 Tension) is unreachable under either extreme: seven BFS-shortest paths measure ~450 turns, which is 90 Tension of CHR-04 decay, so a station-less clock bot arrives with ~10 and no BAL-08 knob lifts it to 60; winding at every station arrives with ~92. | S2 winds the spring at the floor's Winding Station once CHR-03's first warning has fired — Tension <= 30 — and walks the BFS-shortest path to the stairs otherwise. Measured over 200 seeds: median 79, mean 78.7, min 60, max 95, all 200 runs arriving. | PLN-07.1 rule 1: prefer the reading under which the cited ACC test (ACC-130) passes. 30 is not an invented number — it is CHR-03's yellow warning, UI-13's first travel stop, and where BAL-01's own model puts the player when they reach the station (its pre-station low points of 30-39 on floors 2-6 are described as crossing the CHR-03 warnings, "the moment the game asks 'station first, or loot first?'"). | M11 |
| D-099 | BAL-07 S2, UI-13, WLD-08, FLR-08 | Should the S2 clock bot's "BFS-shortest path" cross WLD-08 hazards? The literal shortest path does, and FLR-08's Pendulum band makes crossing floor 7 unavoidable. | The path avoids hazards when another one exists (UI-13's own rule) and crosses them only when the target is otherwise unreachable; a step that would enter a *cyclic* hazard on the turn it fires waits one turn instead (OVR-05's telegraph rule). Without this, 2 of 200 runs died to Grinding Gears on floor 7 at level 1 and measured nothing. | UI-13 already fixes the player's travel passability as hazards avoided unless no other path, so reusing it needs no new rule; and a bot that dies before floor 8 cannot report its arrival Tension. Hazards cost Integrity, never Tension (CHR-04), so the choice does not bias what S2 measures. | M11 |
| D-100 | BAL-07 S3, BAL-02 | BAL-07's S3 sentence never says the bruteforce bot picks anything up. With pickup and equipment disabled it dies on floors 1-2 in 40% of runs, which contradicts the check's own parenthetical ("the boss or floor 4 should stop a skill-less bruteforce"). | S3 collects the items it has seen and wears the best of them. It still never uses a skill, a station or a Spring-Key. Measured over 200 seeds: 90% die on floors 3-5 (128 on floor 3, 30 on floor 4, 22 on floor 5), median death floor 3. | PLN-07.1 rule 1, plus BAL-02: the Integrity budget this check validates is built on the "typical progression" table, which assumes Plating 1 by mid-floor-1 and Plating 2 by floor 2. A bot without that is testing a character the balance model never described. | M11 |
| D-101 | BAL-07 S3/S4, BST-04, ENM-06 | "Attacks the nearest enemy" and "fights everything" leave the target order open. Nearest-first makes floor 3 unwinnable in a different way: BST-04's Downbeat keeps four Music-box Dancers on the board, so the bot fights summons for ever and never the summoner (a measured run broke 292 enemies on floor 3 and then wound down). | A visible boss outranks every other target regardless of distance, and a boss is never written off by the chase timeout of D-102. | It is the only order under which BST-04's summon cap is a fight rather than an infinite XP source, and it is what BAL-06's build table assumes throughout: every cell answers the boss, not its summons. | M11 |
| D-102 | BAL-07 S3/S4, ENM-05, ENM-06 | ENM-06's SKIRMISHER retreats when Tick closes, and Tick and the Cuckoo are both NORMAL, so a melee bot that keeps chasing one across open floor never catches it: a measured run spent 700 turns on floor 3 doing exactly that and wound down. | A chase that goes 12 consecutive turns without the target losing Integrity or Tick getting closer is abandoned for the rest of that floor. Bosses are exempt (D-101). | Twelve is longer than ENM-05's nine-turn `lastKnownAge` memory, so a chase that is genuinely closing is never dropped; and without some bound the bot measures the pathfinder rather than the balance. | M11 |
| D-103 | BAL-07 S4, BAL-02, BAL-06, ENM-06, OVR-05 | "Steps away from telegraphs" does not say which telegraphs or how often. Dodging every wind-up every turn deadlocks against a BRUISER, which re-winds the moment it closes: measured runs spent 2,300-3,900 turns on floor 6 dodging the Regulator without landing a hit. Dodging a SKIRMISHER's ranged wind-up also stops the bot ever closing on a Cuckoo. | Only the heavy hits BAL-02 names are dodged — an enemy with a `heavyAttack` (ENM-06 BRUISER) and the Understudy's Overwind and Pulse — and only on alternate turns, so the pattern is dodge, then swing. | BAL-02 names the list exactly: "stepping away from telegraphed hits (Golem, Regulator, Understudy Overwind/Pulse) removes 5-15 per boss from these figures". BAL-06's answer to the Cuckoo is the opposite one, "Close with Braced Frame; its 1d4 is small". Alternating is the skill expression OVR-05 describes. | M11 |
| D-104 | BAL-07 S4, ENM-08 | "Visits every room" needs a goal that outlives one turn. Re-choosing the Chebyshev-nearest unvisited room every turn makes Tick oscillate between two tiles for ever whenever the nearest room flips back and forth along the path — a measured run bounced between two tiles for 2,300 turns. | One travel goal at a time (station, remembered item, unvisited room, frontier, stairs), kept until it is reached, invalidated, or 120 turns pass, after which its tile is written off for that floor. | 120 is twice ENM-08's 60-step path cap, which bounds the longest legal route across a 60x24 floor, so a goal that is actually being approached is never dropped. Without commitment the bot measures an oscillation rather than a run. | M11 |
| D-105 | BAL-07 S4, WLD-05 | A `Floor` object lists every room, including rooms Tick has never seen. May the explorer bot read them? | No. The bot targets only rooms with at least one tile in `floor.memory`, and otherwise walks to the nearest **frontier** — a remembered walkable tile, or a remembered closed door, with an unseen neighbour. | WLD-05's memory is the game's whole information model, and the bot already reads the station and the stairs through it. Reading unseen room rectangles would let the bot know more than the game ever shows a player, which would make its win rate a measurement of nothing. A closed door counts as a frontier because WLD-05 stops the shadowcast at one. | M11 |
| D-106 | ACC-131, BAL-01, CHR-03, BAL-07 S4 | ACC-131 is a "full-explore scripted run" with no Spring-Keys that must arrive on floor 8 with 35-65 Tension. The S4 bot's station rule is "uses the station on sight", which winds at whatever Tension the station happens to be seen at and therefore wastes most of it; with Spring-Keys disabled that run winds down on floor 5. | ACC-131 runs the S4 policy with `springKeys: false` and the station taken at CHR-03's first warning (Tension <= 30) rather than on sight. Measured on TEST1234: 2,025 turns, 0 Spring-Keys, lowest Tension 10, floor 8 entered with **47**. | PLN-06 calls ACC-131 an "S4-*style*" run, not S4 itself, and its target is derived from BAL-01, whose model assumes the Winding Station "is reached at 50% of a floor's turns" — i.e. at the pre-station low points of 30-39. Winding at 30 is that assumption expressed as a rule, and the measured 47 lands within 2 of BAL-01's modelled 49 for floor 8. | M11 |
| D-109 | ITM-09, DIF-03 | DIF-03 writes the repair's state as `tick.repair = {turnsLeft, perTurn}`, but `solderAmount` does not always divide by `solderTurns` — Efficient Springs' bonus is what makes it uneven, and the rule says the remainder is paid on the last turn. | The state carries a third field, `left`: the untouched remainder. Each turn pays `min(perTurn, left)`, and the last turn pays all of `left`. | One field does the work of an even share and a remainder both, and it keeps the invariant that a repair pays exactly its total — which is what ACC-140 and ACC-143 assert. | M13 |
| D-110 | WLD-14, ENM-03, DIF-06 | A wanderer arrives Active at CMB-02 step 2, before the enemy phase of the same turn. Does it act on the turn it arrives? | No: it is flagged `wokeThisTurn`, so it gains no energy that turn, exactly as ENM-03 treats an enemy that wakes. | ENM-03's rule exists so that nothing ever appears and hits in the same turn; a wanderer is the strongest case for it, since the player cannot have seen it coming. | M13 |
| D-111 | WLD-15, WLD-11, FLR-08, DIF-12 | DIF-12 says every door of the cache room becomes a lock, and that a corridor opening with no door has one forced. Applied literally this can cut the floor in half: corridors run *along* room walls as well as into them, and on floor 7 the Pendulum band can run past a cache room, which is a way in that no lock can close. | The cache role is chosen from the same preference order as before (dead ends, farthest first) but skips any room that cannot be **sealed** (a non-door, non-corridor passable boundary tile) or whose sealing would make another room's centre unreachable from the start without opening a lock. Generation then locks every passable boundary tile of the chosen room, and WLD-11 step 10 re-checks that the stairs, station and journal are still reachable with locks treated as walls. | It keeps both halves of the rule true — every cache entrance is a lock (ACC-166, measured over 1,050 floors), and no floor is ever gated behind one — at a cost of 0.05 mean regenerations, well inside S1's 0.2 budget. | M13 |
| D-112 | BAL-07 S2, WLD-14 | BAL-07's S2 bot removes the floor's enemies on arrival. WLD-14 then keeps sending more, and the measured bot died to them on 33 of 100 seeds, which makes an arrival-Tension check meaningless. | S2 removes the floor's enemies after every action, not only on arrival. | "Never fights (enemies removed)" is the check's own definition of the bot; with wanderers it has to be maintained rather than applied once. The removal costs nothing else: wanderers cost Tension only through the turns they add, and this bot walks the shortest path regardless. | M13 |
| D-113 | DIF-03, BAL-07 S4, OVR-05 | DIF-03's bot note says S4 uses Solder "only when no enemy is visible". A bot that fights everything is almost never out of contact: measured at the DIF-02 numbers, S4 spent 4.6% of its turns below 60% Integrity and was out of contact for 8% of those, and died on floor 3 with two Solder in the pack. | S4 gets the note's rule *plus* two things a player has: it **breaks contact** (steps away from what it can see) below 55% Integrity while it holds a Solder and has the spring to spend on the retreat, for at most 10 consecutive turns; and it will start a repair in contact anyway below 35% Integrity, taking whatever ticks land. | DIF-03's stated purpose is "turns 'chug a potion mid-fight' into 'get out and repair'" — a bot that cannot get out is not modelling the rule, it is modelling a bot. The in-contact exception is legal play: CMB-02 step 3 pays the first tick before the enemy phase, so a repair started in a fight is always worth at least its even share. | M13 |
| D-114 | BAL-07 S4, CHR-05, DIF-05 | BAL-07's S4 "uses the station on sight". DIF-05 made winding loud, and winding at 90 Tension throws away nine tenths of the station and rings the floor for nothing. | S4 winds at Tension <= 45. ACC-131's variant keeps CHR-03's own warning, 30. | "On sight" was free advice while winding was silent; with a price on it the sentence describes a mistake, and S4 is supposed to be the fifth-run player (OVR-05), not the first. | M13 |
| D-115 | DIF-04, BAL-07 S4, CAT-06 | DIF-04 makes Salvage pay in throwables — about 16 a run — and its bot note only says the bot should keep them. The measured bot carried every one of them to its death. | S4 throws them: a Tuning Fork into an adjacent heavy enemy, a Grit Bomb or an Oil Flask into a group of two or more, and never when the throw would land within the blast of Tick (CMB-08 stops a throwable at the first actor in its line, so the landing tile is what is checked, not the target). | A bot that hoards the resource a change hands it measures the change as a pure loss. The landing-tile check is not a nicety: without it the bot burned itself with its own Oil Flasks in 15% of its deaths. | M13 |
| D-116 | ACC-131, DIF-01, DIF-14 | DIF-01 asks ACC-131 — a full explore with no Spring-Keys — to arrive on floor 8 with 15-45 Tension, while DIF-14 says the modelled full-explore line "will go negative on floors 7-8 ... that is the intended result". Both cannot hold: measured, the no-Spring-Key run winds down on floor 5-6 at every setting the ladder reached. | ACC-131 becomes the pair of runs the two statements describe. Spending the Spring-Keys it finds, the full-explore run on TEST1234 reaches floor 8 with 67 Tension and never winds down; spending none, it winds down before floor 8. The test asserts both halves. | DIF-14 is the later and more specific statement, and the pair says what M13 is for: the clock is binding, and the springs on the floor are part of it. The 15-45 band belonged to a run that could afford to ignore them. | M13 |
| D-117 | STY-09, STY-11, R1, R10, D-025 | The wave-3 text shipped and was then read end to end. Almost every individual line was good, but the text had one move and made it every time: a short setup, a full stop, and a wry deflating reversal, very often echoing a word across that full stop (*"She said it was too much. It is too much."*, *"Fits every bolt in the tower because she made every bolt in the tower."*). Thirty-five items, sixteen enemies and twelve skills in one rhythm reads as machine-made however well each entry is written. Three further faults: the theme is stated outright four times, which `STY-09` forbids; every character shares Aurelie's syntax, so the Understudy does not sound like a second mind; and internal identifiers had reached the screen (`(CHR-02)`, `(WLD-14)`, and `decayPeriod`/`decayCounter` in an item tooltip). | `STY-11` is added as a sixth binding rule set on range, and two `STY-09` bullets are amended: the voice bullet no longer makes plainness the target, and the UI bullet now bars internal identifiers from player-facing text outright. Twenty-nine strings are rewritten across `24`, `21`, `22`, `20` and the two `src/screens` leaks; seven proposed rewrites were rejected and keep their current text. No entity is renamed. Every rewrite was reviewed string by string and approved by the author before it was applied. | This is a deliberate, authored change to spec prose, which R10 otherwise permits only for typos — so it is recorded here rather than done quietly, and it **supersedes D-025**, whose reasoning ("rewriting three summaries to fit would change spec content, which R10 and the balance protocol both forbid") was correct for an unreviewed edit made to satisfy a character count, and is not the case here. Note what is *not* changed: no rule, number, table value or ID moves, and the skill summaries D-025 was about are untouched. The range rules are written into `STY-11` rather than applied once, so the next text added to the game is held to them. | M14 |
